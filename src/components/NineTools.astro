---
import RegionalPrice from "./RegionalPrice.astro";

const languages = [
  "English",
  "Suomi",
  "Svenska",
  "Dansk",
  "Norsk",
  "Nederlands",
  "Deutsch",
  "Français",
  "Italiano",
  "Português",
  "Español",
];

const cards = [
  {
    tag: "Counter",
    tier: "free",
    title: "Saved row counter",
    body: "A big round button where your thumb already is. Miscount? Undo. Your row count stays with the project.",
    chips: true,
    span: 2,
  },
  {
    tag: "Pattern",
    tier: "free",
    title: "Pattern viewer with row notes",
    body: "PDF or Ravelry pull-in. Annotate rows, mark your place, and return to the spot you saved.",
    tint: "amber",
  },
  {
    tag: "Calc",
    tier: "free",
    title: "Four knitting calculators",
    body: "Cast on, gauge, increases, yardage. The math your knitting already asked you to do.",
  },
  {
    tag: "Ravelry",
    tier: "free",
    title: "Ravelry, the way it should feel",
    body: "Search, save, start a project in one tap. Log in through Ravelry itself. Your password never touches this app.",
    tint: "walnut",
  },
  {
    tag: "Reference",
    tier: "free",
    title: "Reference library",
    body: "Needles, sizes, chart symbols, and abbreviations, ready when you need them.",
  },
  {
    tag: "Photos",
    tier: "pro",
    title: "Progress photos, just for you",
    body: "One folder per project. Full resolution, on your device, nothing uploaded anywhere.",
  },
  {
    tag: "Stats",
    tier: "pro",
    title: "Project stats without the pressure",
    body: "Hours at the needles. Rows per hour. Streaks. No leaderboards, no nudges, no comparing.",
    tint: "sage",
  },
  {
    tag: "Widget",
    tier: "pro",
    title: "A widget on your home screen",
    body: "Tap to count. No app launch, no loading. The exact amount of friction a row counter should have, which is zero.",
    tint: "terracotta",
  },
];

const freeCards = cards.filter((c) => c.tier === "free");
const proCards = cards.filter((c) => c.tier === "pro");
---

<section class="nine-tools" aria-labelledby="nine-heading">
  <div class="nine-inner">
    <header class="section-head" data-reveal>
      <p class="section-label">
        <span class="label-text">Features</span>
      </p>
      <h2 id="nine-heading" class="title">
        Your knitting tools,<br /><em>in one project home.</em>
      </h2>
    </header>

    <p class="manifesto" data-reveal>
      Keep your row counter, pattern viewer, yarn, notes, progress photos, and
      knitting calculators together in one Android project home, so you can pick
      up where you left off.
    </p>

    <div class="tier-head" aria-hidden="true" data-reveal="clip">
      <span class="tier-label">Free forever</span>
      <span class="tier-count">{freeCards.length} tools</span>
      <span class="tier-line"></span>
    </div>

    <div class="nine-grid">
      {
        freeCards.map((c) => (
          <article
            class:list={[
              "tool-card",
              c.tint && `tint-${c.tint}`,
              c.span === 2 && "span-2",
            ]}
            data-reveal="scale"
          >
            <div class="card-meta">
              <span class="tag">{c.tag}</span>
            </div>
            <h3>{c.title}</h3>
            <p>{c.body}</p>
            {c.chips && (
              <ul class="chip-row" aria-label="Supported languages">
                {languages.map((lang) => (
                  <li class="chip">{lang}</li>
                ))}
              </ul>
            )}
          </article>
        ))
      }
    </div>

    <div class="tier-head tier-head-pro" aria-hidden="true" data-reveal="clip">
      <span class="tier-label">Pro <RegionalPrice phase="launch" /></span>
      <span class="tier-count">{proCards.length} tools</span>
      <span class="tier-line"></span>
    </div>

    <div class="nine-grid pro-feature-grid">
      {
        proCards.map((c) => (
          <article
            class:list={[
              "tool-card",
              c.tint && `tint-${c.tint}`,
              c.span === 2 && "span-2",
            ]}
            data-reveal="scale"
          >
            <div class="card-meta">
              <span class="tag">{c.tag}</span>
            </div>
            <h3>{c.title}</h3>
            <p>{c.body}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .nine-tools {
    color: var(--ink);
    padding: 120px 40px;
  }
  .nine-inner {
    max-width: 1280px;
    margin: 0 auto;
  }

  .section-head {
    padding-bottom: 18px;
    border-bottom: 1px solid var(--ink);
    margin-bottom: 28px;
  }

  .manifesto {
    font-family: var(--font-body);
    font-size: var(--landing-body-size);
    line-height: 1.6;
    font-weight: 400;
    color: var(--ink);
    max-width: 580px;
    margin: 0 0 40px;
  }
  .manifesto em {
    font-weight: 500;
    color: var(--terracotta);
  }
  .section-head .section-label {
    display: inline-flex;
    align-items: baseline;
    gap: 20px;
    font-family: var(--font-body);
    font-size: var(--landing-label-size);
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--terracotta);
    margin: 0 0 10px;
  }
  .section-head .title {
    font-family: var(--serif);
    font-weight: 400;
    font-size: var(--landing-section-title-size);
    line-height: var(--landing-section-title-line-height);
    letter-spacing: 0;
    margin: 0;
    color: var(--ink);
  }
  .section-head .title em {
    color: var(--terracotta);
    font-style: normal;
    font-size: 1em;
    line-height: 1;
    display: inline-block;
    margin-top: 6px;
  }

  .tier-head {
    display: flex;
    align-items: baseline;
    gap: 16px;
    margin-bottom: 24px;
    font-family: var(--font-body);
    font-size: var(--landing-small-size);
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }
  .tier-head-pro {
    margin-top: 64px;
  }
  .tier-head .tier-label {
    color: var(--terracotta);
    white-space: nowrap;
  }
  .tier-head .tier-count {
    color: var(--ink-soft);
    white-space: nowrap;
  }
  .tier-head .tier-line {
    flex: 1;
    height: 1px;
    background: var(--ink);
    opacity: 0.4;
  }

  .nine-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
  .nine-grid + .nine-grid {
    margin-top: 20px;
  }
  .nine-grid + .tier-head-pro {
    margin-top: 48px;
  }

  .tool-card {
    background: var(--wheat);
    color: var(--ink);
    border: 1px solid var(--ink);
    padding: 28px 28px 32px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    border-radius: 0;
  }
  .tool-card.span-2 {
    grid-column: span 2;
  }

  .tool-card.tint-sage {
    background: var(--stripe-teal-accessible);
    color: var(--paper);
    border-color: var(--stripe-teal-accessible);
  }
  .tool-card.tint-sage h3 {
    color: var(--paper);
  }
  .tool-card.tint-sage .tag {
    background: var(--paper);
    color: var(--ink);
  }
  .tool-card.tint-sage p {
    color: var(--paper);
  }

  .tool-card.tint-walnut {
    background: var(--walnut);
    color: var(--paper);
    border-color: var(--walnut);
  }
  .tool-card.tint-walnut h3 {
    color: var(--paper);
  }
  .tool-card.tint-walnut .tag {
    background: var(--paper);
    color: var(--ink);
  }
  .tool-card.tint-walnut p {
    color: var(--paper);
  }

  .tool-card.tint-amber {
    background: var(--stripe-rust-accessible);
    color: var(--paper);
    border-color: var(--stripe-rust-accessible);
  }
  .tool-card.tint-amber h3 {
    color: var(--paper);
  }
  .tool-card.tint-amber .tag {
    background: var(--paper);
    color: var(--ink);
  }
  .tool-card.tint-amber p {
    color: var(--paper);
  }

  .tool-card.tint-terracotta {
    background: var(--terracotta);
    color: var(--paper);
    border-color: var(--terracotta);
  }
  .tool-card.tint-terracotta h3 {
    color: var(--paper);
  }
  .tool-card.tint-terracotta .tag {
    background: var(--paper);
    color: var(--ink);
  }
  .tool-card.tint-terracotta p {
    color: var(--paper);
  }

  .tool-card.tint-ink {
    background: var(--ink);
    color: var(--paper);
    border-color: var(--ink);
  }
  .tool-card.tint-ink h3 {
    color: var(--paper);
  }
  .tool-card.tint-ink .tag {
    background: var(--paper);
    color: var(--ink);
  }
  .tool-card.tint-ink p {
    color: var(--paper);
  }

  .card-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .tag {
    font-family: var(--font-body);
    font-size: var(--landing-small-size);
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ink);
  }
  .tool-card h3 {
    font-family: var(--font-body);
    font-weight: 600;
    font-size: var(--landing-card-title-size);
    line-height: 1.15;
    letter-spacing: 0;
    text-transform: none;
    margin: 0;
    color: var(--ink);
  }
  .tool-card p {
    font-family: var(--font-body);
    font-size: var(--landing-body-size);
    line-height: 1.55;
    color: var(--ink);
    margin: 0;
  }

  .chip-row {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    padding: 0;
    margin: 4px 0 0;
  }
  .chip {
    font-family: var(--font-body);
    font-size: var(--landing-small-size);
    line-height: 1.35;
    color: var(--ink);
    background: var(--paper);
    border: 0;
    padding: 4px 11px;
    border-radius: 999px;
  }

  @media (max-width: 1024px) {
    .nine-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .tool-card.span-2 {
      grid-column: 1 / -1;
    }
    .pro-feature-grid {
      grid-template-columns: 1fr;
    }
  }
  @media (max-width: 767px) {
    .nine-tools {
      padding: 80px 20px;
    }
    .section-head .section-label {
      gap: 12px;
    }
    .nine-grid {
      grid-template-columns: 1fr;
    }
    .tool-card.span-2 {
      grid-column: auto;
    }
    .tool-card {
      padding: 24px 22px 28px;
    }
  }
</style>
